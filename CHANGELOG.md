# 版本历史

本项目使用语义化版本号。日期采用 `YYYY-MM-DD` 格式。

## v0.1.1 - 2026-08-23

本地录制与录后编辑工作室版本。

### 单一项目文件夹

- 项目面板统一显示用户自定义的项目路径；移除录制区第二套保存位置入口。
- 项目根目录包含 `project.excalicord.json`、`scene.excalidraw`、`recordings/` 和可选的 `text/subtitles.srt`/`text/subtitles.vtt`。
- 桌面录制和浏览器直存的原始录制写入 `recordings/`，项目媒体清单只记录相对路径、类型、时间和时长。
- 增加受限的项目资产读写与媒体流接口；旧 `save-folder` 接口保留兼容。浏览器直存依赖 `showDirectoryPicker`。
- 新建项目时立即创建 `recordings/`；空字幕会删除旧 SRT，缺失项目文件和取消文件夹选择不再作为控制台错误。
- `scene.excalidraw` 保存实时元素与图片文件，只保留可安全恢复的背景、网格、主题和视口状态，过滤协作与编辑瞬时状态。
- 项目区按“项目 → 提词器/讲解准备 → 摄像头与麦克风 → 录制”排列；提供“设置项目文件夹…”“打开项目文件夹…”“在 Finder 中显示”“打开 Excalidraw 文件…”和“保存白板”显性命令。设置根目录不再隐式替换当前画布，单文件入口保持项目根不变。
- 项目文件接入 schema v2：打开时支持 v2 规范化和 v1 → v2 迁移，运行态继续兼容 v1 localStorage；保存时通过 EditorCore 合并运行态并保持既有项目、录制和编辑 ID。
- 停止录制后统一称为“原始录制”，当前动作是“保存录制”或“播放原始录制”；只有经过录后编辑和渲染的文件才称为“成片”。
- 打开白板时同步恢复图片附件与项目数据；保存白板时同步写入场景、附件、项目清单和字幕。

### 录制会话与智能镜头

- 增加本地录制项目元数据和会话事件模型，不保存或上传原始媒体；
- 记录录制开始、暂停、继续、停止、Frame 切换、鼠标位置和点击事件；
- 增加白板/当前幻灯片录制的“智能镜头”开关和轻微、适中、明显三档强度；
- 智能镜头在白板画布录制中平滑跟随鼠标焦点，并在画面中同步调整光标高亮位置；
- 增加 v0.1.1 项目状态与受限资产接口，供录后编辑、自动保存和端到端验收使用。
- 新录制按稳定安全的 `recordings/<session-id>/` 归档，并写入带明确 `recording-start` 毫秒时基的 `session.json` 与 `events.json`；旧 `recordings/recording.mp4` 保持兼容，不移动原片。
- Native 项目资产写入白名单覆盖录后文本资产和会话元数据，拒绝路径穿越及任意路径；当前混合录制只登记真实媒体资产，并明确标记 `legacyComposite` 限制。

### 录后编辑工作台

- 增加独立全屏录后编辑器，提供视频预览、原片/成片时间映射、时间线、入点/出点剪切、恢复、撤销、重做和自动保存。
- 所有剪辑均为非破坏式 edit，原始录制只读；重新打开项目后可恢复剪切、字幕、镜头、光标、摄像头、画面和音频配置。
- 增加本地渲染管线：剪切/速度、智能镜头、动态光标与点击环、独立摄像头素材、中文字幕、背景、圆角、留白、阴影、音量和淡入淡出，输出 `exports/final.mp4`。
- 预览与导出共享 schema v2 组合数据；本地媒体接口支持 Range 请求，并限制在当前项目根目录内。

### 逐字稿、字幕与提词器

- 提词器讲稿保存到本地 v0.1.1 项目模型；
- 增加“保存为讲稿”和“载入讲稿”操作；
- 录制前仅支持“从 SRT/VTT 导入讲稿”，导入内容直接保存为 script 并只用于提词，不写入 subtitle track；
- 增加本地 faster-whisper 词级转写服务，识别结果来自实际录音，支持 Frame/白板文字和讲稿术语上下文。
- 原始逐字稿、校正版和修改记录分别保存到 `text/transcript.raw.json`、`text/transcript.corrected.json`、`text/transcript.corrections.json`，校对不会覆盖原始 ASR。
- 可从校正版生成字幕、逐条编辑，并保存/导出 SRT 与 VTT；字幕烧录支持中文字体。
- 保持提词稿、实际逐字稿和字幕三个模型分离；提词稿只用于录制辅助和术语上下文。

### 智能粗剪与安全审计

- 增加面向中文口播的保守剪辑建议：前摇、收尾、长停顿、重说、相似表达和填充词；AI 只提出建议，用户接受后才写入 cut track。
- 数字、英文术语、单位、逻辑信息默认进入信息损失审计。
- 长静音同时检查 Frame 切换、点击、指针和白板编辑事件；包含画面演示时降级为“需复核”，不会把“没有说话”直接等同于“没有内容”。
- 重新分析会与现有 cut 和人工决定对齐；恢复剪切后不会错误保留“已剪切”状态。

### 验收与能力边界

- 已通过真实浏览器设置项目目录、保存/重开 284 元素白板、桌面录制、Frame/鼠标事件回读、校正版逐字稿、SRT/VTT、非破坏剪辑和最终 MP4 导出。
- 实际成片验证覆盖 H.264/AAC 解码、中文字幕、智能镜头、动态光标与点击环、画面包装和音频淡入淡出。
- 当前 Native Capture Agent 的新录制仍是混合媒体，已烧入原片的摄像头和音频层不能在录后拆分；项目与界面必须如实显示 `legacyComposite` 限制。
- 浏览器控制、摄像头权限和最终播放器观感仍需在目标 macOS 用户会话中复验；自动测试不能替代真实回放。

## v0.1.0 - 2026-08-21

初版发布版本。

### 核心能力

- 将 Excalidraw 同一张白板中的 Frame 组织成可导航的幻灯片；
- 提供幻灯片导航、总览、搜索、重命名、删除和排序；
- 提供白板总览、聚焦、缩放、撤回、前进和演示播放控制；
- 支持三种录制范围：屏幕/窗口、白板画布、当前幻灯片；
- 支持摄像头画中画，以及将摄像头画中画合成进导出视频；
- 支持提词器、录制快捷键、保存位置和本地原始录制验收；
- 支持本地场景保存和部署一致性检查。

### 部署与文档

- 提供中文 README、快速开始、安装部署、使用说明、常见问题和开发同步文档；
- 提供 `npm run preflight` 部署前检查；
- 提供 `npm run configure:local` 和 `.env.local` 本机运行目录配置；
- 提供 Codex、Claude Code、Cursor、ChatGPT Desktop 等本地大模型辅助部署提示词；
- 提供公开示例白板、PDF、SVG 和 README 真实插件截图。

### 验收边界

- 自动检查用于确认源码和部署链路没有明显断裂；
- 录制、摄像头画中画、摄像头合成进视频和不同录制范围，仍以真实浏览器操作、实际导出文件和本地播放器回放为最终验收。
